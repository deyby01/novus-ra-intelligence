# ============================================================
# dev.txt - local development and CI ONLY.
# The production image never installs this file.
# ============================================================

-r base.txt

# --- Testing (the heart of TDD) ---
pytest~=8.3                # test runner
pytest-django~=4.11        # integrates pytest with Django (db fixture, settings)
factory-boy~=3.3           # test object factories (better than JSON fixtures)
pytest-cov~=6.0            # coverage reporting

# --- Code quality ---
ruff~=0.11                 # linter + formatter (replaces flake8/black/isort)
pre-commit~=4.0            # runs ruff automatically before every commit
