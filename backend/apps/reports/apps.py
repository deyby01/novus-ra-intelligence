from django.apps import AppConfig


class ReportsConfig(AppConfig):
    """Django app config for the reports app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.reports"
