from django.apps import AppConfig


class ActivityConfig(AppConfig):
    """Django app config for the activity app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.activity"
